import React from "react";
import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    container: {
        flex: 1,
        padding: 15,
        marginTop: 15,
        border: 'solid',
        borderWidth: 1,
        borderTopColor: '#fff',
        borderRadius: 15,
        fontWeight: 'Bold',
    },
    Title:{
        color: '#fff',
        fontSize: 15,
    },
})